server:
  port: 7070
spring:
  application:
    name: ahcc-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/alexanderhuahuacccapacc/Examen_Unidada_1.git # ruta del repositorio git
          searchPaths: config-data
          default-label: main
  security:
    user:
      name: root
      password: 123456